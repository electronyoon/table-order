services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: table_order
      POSTGRES_USER: table_order
      POSTGRES_PASSWORD: table_order
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
