package com.electronyoon.tableorder.domain.busymode;

import org.springframework.data.jpa.repository.JpaRepository;

public interface BusyModeSettingRepository extends JpaRepository<BusyModeSetting, Long> {
}
